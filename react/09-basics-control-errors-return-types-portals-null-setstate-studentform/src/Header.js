// import React from 'react';

// You can return strings without needing to wrap e.g. inside <p></p> - They'll render as text nodes --> we don't even need to import react
const Header = () => "Hello from the Header!";

export default Header;
