package com.teamtreehouse;

// Interface: "interface" replaces "abstract class". Only declare constants (no need to add "static final", they are
// always this way by default) and abstract methods (no need to add "abstract", they are always this way by default).
public interface Chattable {
    String chat();
}
